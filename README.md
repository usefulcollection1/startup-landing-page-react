# startup-landing-page-react

🚀 A modern startup landing page built with React + Vite.

Features
- Modular components: Hero, Features, Card, Footer
- Uses props and `Array.prototype.map()` for dynamic rendering
- Responsive grid layout with clean CSS styling

This project was bootstrapped from a React + Vite template and adjusted to include project-specific assets and components.
